
namespace Week6Notes
{
    public enum WeekDay //changed this just to not break the code in progam (was previously DayOfWeek)
    {
        Sunday, //if you change the number of the 1st value it will change all the rest +1
        Monday,
        Tuesday,
        Wednesday,
        Thursday,
        Friday,
        Saturday
    }
}
